name: Build and Obfuscate

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        name: Install pnpm
        with:
          version: 10
          run_install: false

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 23
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Create dist directory
        run: mkdir -p dist

      - name: Minify JavaScript
        run: pnpm minify

      - name: Prepare release files
        run: pnpm prepare-release

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: |
            dist/
            script.min.js
          retention-days: 30

      - name: Verify obfuscated file
        run: |
          echo "Original file size:"
          wc -c script.js
          echo "Minified file size:"
          wc -c dist/script.min.js
          echo "Obfuscated file size:"
          wc -c dist/script.obfuscated.js
          echo "Final release file size:"
          wc -c script.min.js
